{
  "name": "combo-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "data": "node scripts/build-names.mjs && node scripts/build-catalog.ts",
    "build": "npm run data && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.ts"
  },
  "engines": {
    "node": ">=22.18"
  },
  "devDependencies": {
    "color-name-list": "^14.51.0",
    "lucide-static": "^1.47.0",
    "typescript": "^5.9.3",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/big-shoulders-display": "^5.3.0",
    "fflate": "^0.8.3"
  }
}
